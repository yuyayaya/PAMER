<% provide(:title, "Log in") %>
<div>
  <h1 class="mt-3">Log in</h1>
    <div class="row justify-content-center">
      <div class="col-md-5 col-md-offset-4">
        <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
        <%= render "devise/shared/error_messages", resource: resource %>

        <div class="field">
          <%= f.email_field :email, placeholder: "メールアドレス", class: "mt-3", autofocus: true, autocomplete: "email" %>
        </div>

        <div class="field">
          <%= f.password_field :password, placeholder: "パスワード", class: "mt-3", autocomplete: "current-password" %>
        </div>

        <% if devise_mapping.rememberable? %>
        <div class="field">
          <%= f.label :remember_me, class: "checkbox inline" do %>
            <%= f.check_box :remember_me, checked: false %>
            <span>Remember me</span>
          <% end %>
        </div>
        <% end %>

        <div class="actions">
          <%= f.submit "Log in", class: "btn btn-primary btn-block rounded-pill mt-3" %>
        </div>
        <% end %>

        <%= link_to user_facebook_omniauth_authorize_path,
          class: "btn btn-primary mt-2 rounded-pill" do %>
          <%= image_tag("flogo_RGB_HEX-114.png", alt: "facebook logo",
            width: '35', height: '35', class: "rounded-circle") %>
          Log in with facebook
        <% end %>
    </div>
  </div>
</div>
